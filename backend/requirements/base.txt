Django>=5.0,<5.1
djangorestframework>=3.15,<3.16
djangorestframework-simplejwt>=5.3,<5.4
psycopg[binary]>=3.1,<3.2
django-redis>=5.4,<5.5
redis>=5.0,<6.0
django-cors-headers>=4.3,<4.4
django-environ>=0.11,<0.12
Pillow>=11,<13
django-ratelimit>=4.1,<4.2

# --- Background tasks ---
celery>=5.4,<5.5

# --- PDF engine (apps.pdfgen) ---
# Versions verified against the V_1.0 venv that produced the archived PDFs
# (.venv/lib/python3.14/site-packages/*.dist-info).
reportlab==5.0.1
qrcode[pil]==8.2
arabic-reshaper==3.0.1
# PINNED DELIBERATELY. The renderer imports `from bidi.algorithm import
# get_display` — the legacy pure-Python implementation. python-bidi 0.6.x also
# ships a Rust `bidi.get_display` whose output differs in edge cases, and 0.7.x
# removes `bidi.algorithm` entirely. Changing this silently changes Persian
# shaping, so newly generated PDFs would stop matching already-issued ones.
python-bidi==0.6.11

# --- Jalali dates ---
jdatetime==6.1.0

# --- V_1.0 importer (apps.importer) ---
# Read-only access to V_1.0's MongoDB, only when `manage.py import_v1 --mongo-uri-env`
# is used; imported lazily so nothing else depends on it.
pymongo==4.10.1
